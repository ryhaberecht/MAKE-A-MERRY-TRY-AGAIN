{
    "manifest_version": 2,
    "name": "MAKE FIREFOX TRY AGAIN",
    "version": "1.0",

    "description": "If loading a page fails, try again after x seconds. The reload bypasses the browser cache. This Add-on was inspired by the sadly discontinued Add-on TryAgain, but is in no way connected to it or its authors. Feel free to view and change the source code on: ",

    "permissions": [
        "webRequest", "<all_urls>", "tabs", "storage"
    ],
    
    "options_ui": {
        "page": "options.html",
        "browser_style": true
    },
    
    "background": {
        "scripts": ["background.js"]
    }
}
